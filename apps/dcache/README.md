# @neriros/dcache

A distributed task cache for Nx monorepos. Speeds up builds by caching task outputs and restoring them when inputs haven't changed.

## Install

```bash
npm i -g @neriros/dcache
```

## Usage

```bash
# Run a task with caching
dcache run <task>

# Show help
dcache --help
```
